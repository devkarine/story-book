import { Meta } from "@storybook/blocks";
import { ColorsMap } from "../components/ColorsMap";

<Meta title="Tokens/Colors" />

# Colors

Colors are defined in the `colors` object in the theme. The keys are the color names and the values are the color values.

<ColorsMap />
